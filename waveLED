void setup() {
  pinMode(1, OUTPUT);
  pinMode(2, OUTPUT);
  pinMode(3, OUTPUT);
  pinMode(4, OUTPUT);
  pinMode(5, OUTPUT);
}

void loop() {
  for(int led = 1; led <= 5; led++){
    digitalWrite(led, HIGH);
    delay(10);
    digitalWrite(led,LOW);
  }
}
